server:
  port: 8092
spring:
  datasource:
    url: jdbc:postgresql://192.168.1.50:5432/mariasorganicsdb
    driverClassName: org.postgresql.Driver
    username: root
    password: root
    hikari:
      auto-commit: false
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
